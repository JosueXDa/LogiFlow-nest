apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-service
  namespace: logiflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tracking-service
  template:
    metadata:
      labels:
        app: tracking-service
    spec:
      containers:
      - name: tracking-service
        image: logiflow/tracking-service:v2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3007
          name: tcp
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: TRACKING_SERVICE_PORT
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: NODE_ENV
        - name: TCP_HOST
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: TCP_HOST
        - name: TCP_PORT
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: TRACKING_TCP_PORT
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: TRACKING_DATABASE_URL
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: RABBITMQ_URL
        - name: RABBITMQ_USER
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: RABBITMQ_DEFAULT_USER
        - name: RABBITMQ_PASS
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: RABBITMQ_DEFAULT_PASS
        resources:
          requests:
            memory: "100Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  namespace: logiflow
spec:
  type: ClusterIP
  ports:
  - port: 3007
    targetPort: 3007
    protocol: TCP
    name: http
  - port: 4008
    targetPort: 4008
    protocol: TCP
    name: tcp
  selector:
    app: tracking-service
