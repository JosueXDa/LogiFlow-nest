apiVersion: apps/v1
kind: Deployment
metadata:
  name: pedidos-service
  namespace: logiflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pedidos-service
  template:
    metadata:
      labels:
        app: pedidos-service
    spec:
      containers:
      - name: pedidos-service
        image: logiflow/pedidos-service:v2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3002
          name: tcp
        env:
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: PEDIDOS_SERVICE_PORT
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: NODE_ENV
        - name: TCP_HOST
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: TCP_HOST
        - name: TCP_PORT
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: PEDIDOS_TCP_PORT
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: PEDIDOS_DATABASE_URL
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: logiflow-config
              key: RABBITMQ_URL
        - name: RABBITMQ_USER
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: RABBITMQ_DEFAULT_USER
        - name: RABBITMQ_PASS
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: RABBITMQ_DEFAULT_PASS
        resources:
          requests:
            memory: "100Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: pedidos-service
  namespace: logiflow
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  - port: 4004
    targetPort: 4004
    protocol: TCP
    name: tcp
  selector:
    app: pedidos-service
