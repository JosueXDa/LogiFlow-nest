apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-fleet
  namespace: microservices-study
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-fleet
  template:
    metadata:
      labels:
        app: postgres-fleet
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - configMapRef:
                name: fleet-db-config
            - secretRef:
                name: fleet-db-secret
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-fleet-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-fleet
  namespace: microservices-study
spec:
  selector:
    app: postgres-fleet
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-fleet-pvc
  namespace: microservices-study
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: fleet-db-config
  namespace: microservices-study
data:
  POSTGRES_DB: fleet_db
  POSTGRES_USER: postgres
---
apiVersion: v1
kind: Secret
metadata:
  name: fleet-db-secret
  namespace: microservices-study
type: Opaque
stringData:
  POSTGRES_PASSWORD: postgres
