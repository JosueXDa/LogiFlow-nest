apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-pedidos
  namespace: microservices-study
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-pedidos
  template:
    metadata:
      labels:
        app: postgres-pedidos
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - configMapRef:
                name: pedidos-db-config
            - secretRef:
                name: pedidos-db-secret
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pedidos-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-pedidos
  namespace: microservices-study
spec:
  selector:
    app: postgres-pedidos
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pedidos-pvc
  namespace: microservices-study
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: pedidos-db-config
  namespace: microservices-study
data:
  POSTGRES_DB: pedidos_db
  POSTGRES_USER: postgres
---
apiVersion: v1
kind: Secret
metadata:
  name: pedidos-db-secret
  namespace: microservices-study
type: Opaque
stringData:
  POSTGRES_PASSWORD: postgres
